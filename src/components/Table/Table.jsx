import React from 'react'
import BootstrapTable from 'react-bootstrap-table-next';

const Table = ({posts}) => {
    console.log(posts)
    return (
        <div>table</div>
    )
};

export default Table;
